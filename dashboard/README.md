# Тендерный штаб

Локальная рабочая сводка для менеджеров по тендерным продажам.

## Запуск

Открывайте проект через локальный HTTP-сервер из корня `ИИ-Агент Тендер`, чтобы интерфейс мог загрузить `dashboard/data/tenders.json`.

## Ежедневное обновление

Агент должен:

1. Сформировать новый Excel в `reports/tenders`.
2. Обновить `dashboard/data/tenders.json`.
3. Сохранить неизменными идентификаторы существующих закупок.
4. Не перезаписывать вручную установленные статусы менеджеров.
5. Переносить завершенные закупки в будущий архивный набор данных.

Статусы первой версии хранятся в `localStorage` браузера по ключу `prs-tender-manager-status-v1`.
